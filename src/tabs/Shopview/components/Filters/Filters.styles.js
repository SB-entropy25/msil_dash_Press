import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  "shopView-right-info": {
    display: "flex",
    alignItems: "center",
    gap: "1rem",
  },
}));
export default useStyles;
